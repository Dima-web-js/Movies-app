## 1) Распространение событий в JS (Event Propagation)

  **Может использоваться для:**

  - Capturing (перехват): событие идет сверху вниз: window → document → html → body → … → целевой элемент.
  - Target (цель): событие достигает целевого элемента.
  - Bubbling (всплытие): идет снизу вверх обратно к window.

  **Подписка на фазу:**

  ```javascript

  // Перехват (capturing): третий аргумент true или { capture: true }
  elem.addEventListener('click', handler, true);

  // Всплытие (по умолчанию)
  elem.addEventListener('click', handler);

  // Современные опции
  elem.addEventListener('click', handler, { capture: true, once: true, passive: true });
  ```


  **Остановка распространения:**

  ```javascript
  function handler(e) {
    e.stopPropagation();           // дальше по дереву не идет
    // e.stopImmediatePropagation(); // останавливает и другие обработчики на том же элементе
    // e.preventDefault();           // отменяет действие по умолчанию (если оно есть)
  }
  ```


  **Небуферизующиеся (не всплывают) события (важно знать):** focus, blur, mouseenter, mouseleave и некоторые кастомные — для них используйте capturing или делегирование на родителе через аналоги (focusin/focusout всплывают).

  **Shadow DOM нюанс:** путь события — e.composedPath(). Часть пути может быть скрыта инкапсуляцией.

  **Практические применения:**

  **Делегирование событий:** один обработчик на контейнер вместо сотен на дочерних элементах.

  ```javascript
  document.querySelector('#list').addEventListener('click', (e) => {
    const item = e.target.closest('[data-item-id]');
    if (!item) return;
    console.log('Clicked id=', item.dataset.itemId);
  });
  ```


  **Перехват для глобальных правил/UI-китов:** логирование, аналитика, горячие клавиши (на document с capture: true).

  **Контроль конфликтов:** stopPropagation/stopImmediatePropagation в виджетах, чтобы сторонние либы не «перехватывали» клики.

  **Производительность:** делегирование уменьшает количество обработчиков → меньше памяти и вызовов GC.

  **Пассивные слушатели для скролла/тача:** { passive: true } говорит движку, что вы не вызовете preventDefault(), и он может оптимизировать прокрутку.

  **Различные фазы для UX:** например, на capture отменить нежелательные действия до того, как их перехватят другие.

  **Я лично часто использую в для остановки распространения событий**, например, если карточка, кликая по ней открывается модальное окно, но сбоку у неё есть корзина, которая вызывает удаление. Чтобы предотвратить конфликт между удалением и всплытием модального окна я использую stopPropagation().

## 2) Promise — это «коробка с будущим результатом».

  **Он бывает в трёх состояниях:**

  pending — ещё не готово;

  fulfilled — успешно, есть значение;

  rejected — ошибка.

  После fulfilled или rejected промис становится завершённым (settled) и состояние уже не меняется.

  **Мини-пример:**

  ```javascript
  const p = new Promise((resolve, reject) => {
    setTimeout(() => resolve('OK'), 100);
  });

  p.then(value => console.log(value))     // 'OK'
  .catch(err => console.error(err))
  .finally(() => console.log('done'));
  ```

  **В работе они нужны для:**

  - Загрузки данных: fetch('/api/products').

  - Таймеры/ожидание: подождать 500 мс перед повтором запроса.

  - Работа с файлами/базами в браузере: File System Access API, IndexedDB (обёртки).

  - Анимации и переходы состояний: дождаться конца транзиции/анимации.

  - Параллельные операции: сразу получить и юзера, и его заказы.

  - Ограничение времени (таймаут) и отмена: если сервер «молчит», оборвать запрос.

  - Два стиля записи: цепочки .then и async/await

  Оба способа эквивалентны, разница — в читаемости.

  **Цепочка**
  ```javascript
  fetch('/api/user')
    .then(r => r.json())
    .then(user => user.name)
    .then(name => console.log(name))
    .catch(console.error);
  ```

  **async/await (чаще читается легче)**
  ```javascript
  async function loadUser() {
    try {
      const r = await fetch('/api/user');
      if (!r.ok) throw new Error('HTTP ' + r.status);
      const user = await r.json();
      return user.name;
    } catch (e) {
      console.error(e);
      return 'anonymous';
    } finally {
      console.log('done');
    }
  }
  ```

  **Типовые задачи и готовые шаблоны**

  **1. Последовательные шаги (строго один за другим)**
  ```javascript
  async function createOrderAndPay() {
    const order = await api.createOrder(); // сначала создать
    const payment = await api.pay(order.id); // потом оплатить
    return payment.status;
  }
  ```


  Используется, когда второй шаг зависит от результата первого.

  **2. Параллельно (быстрее, если шаги не зависят)**
  ```javascript
  async function loadPageData() {
    const [user, products] = await Promise.all([
      fetch('/api/user').then(r => r.json()),
      fetch('/api/products').then(r => r.json()),
    ]);
    return { user, products };
  }
  ```


  Важно: два await подряд — последовательно, Promise.all — параллельно.

  **3. «Кто успел — тот съел» (любой успешный результат)**
  ```javascript
  const fastest = await Promise.any([
    fetch('/api/mirror1'),
    fetch('/api/mirror2'),
    fetch('/api/mirror3'),
  ]);
  // берём первый УСПЕШНЫЙ ответ (ошибки игнорит, пока не все упадут)
  ```


  **4. Таймаут запроса (не висим бесконечно)**
  ```javascript
  function timeout(ms) {
    return new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), ms));
  }

  async function getWithTimeout(url, ms = 5000) {
    return await Promise.race([ fetch(url), timeout(ms) ]);
  }
  ```


  **5. Последовательный цикл (если порядок критичен)**
  ```javascript
  const ids = [1,2,3,4,5];
  const items = [];
  for (const id of ids) {
    const it = await fetch(`/api/item/${id}`).then(r => r.json());
    items.push(it); // гарантированно по порядку
  }
  ```

  **6. Прогресс/стриминг ответа (крупные данные)**
  ```javascript
  const r = await fetch('/big-file');
  const reader = r.body.getReader();
  let received = 0;

  while (true) {
    const { value, done } = await reader.read();
    if (done) break;
    received += value.length;
    console.log('progress', received);
  }
  ```


  **Event Loop**

  Event Loop — «оркестр», который решает, что выполнять дальше.

  **У него есть:**

  Call Stack — что выполняется прямо сейчас.

  Macrotask queue — «крупные задачи» (setTimeout, setInterval, MessageChannel, I/O).

  Microtask queue — «микрозадачи» (Promise.then/catch/finally, queueMicrotask, MutationObserver).

  Правило: после каждой macrotask движок вычищает ВСЕ microtasks, потом может перерисовать кадр.

  **Мини-пример порядка:**

  ```javascript
  console.log('A');
  setTimeout(() => console.log('B'), 0);            // macrotask
  Promise.resolve().then(() => console.log('C'));   // microtask
  console.log('D');
  // Порядок: A, D, C, B
  ```


  Анимации: requestAnimationFrame запускается перед отрисовкой кадра (обычно ~ каждые 16.7 мс при 60 FPS).

  **Практические следствия Event Loop**

  Promise.then(...) всегда выполнится раньше, чем setTimeout(..., 0).

  Длинные цепочки microtask’ов (бесконечные then) могут «подвесить» кадр и вызвать лаги.

  Для «перекинуться на следующий тик» и не блокировать кадр — иногда наоборот полезно setTimeout(...,0).

  **Другие способы асинхронности (когда промиса мало)**

  Callbacks — старый стиль: fn((err, res) => {}). Работает, но плохо читается.

  Observables (RxJS) — потоки во времени: поисковая строка с дебаунсом, «повторы до успеха», отмена по unsubscribe.

  Web Workers — знаю, что они есть, но с ними ещё не работал.

  Streams API — работать с данными кусочками (видео/аудио/большие файлы). Тоже знаю, что они есть, но с ними ещё не работал.

## 3) ООП
  **ООП (объектно-ориентированное программирование)** — это парадигма, в которой программа строится вокруг объектов, объединяющих данные (свойства) и поведение (методы).
  Главная цель — сделать код более модульным, переиспользуемым и понятным.

  **ООП в JS/TS** — это способ структурировать код вокруг объектов с данными и поведением, используя принципы инкапсуляции, наследования, полиморфизма и абстракции, где TypeScript добавляет строгую типизацию и классическую структуру, а JS под капотом опирается на прототипы.

  Практически во всех современных фреймворках, включая (реакт, ангуляр, vue.js и т.р.) ts стал новым стандартом из-за более строгой типизации в отличие от js. 

  **🔹 Основные принципы ООП**

  **1. Инкапсуляция**
  Сокрытие внутренней реализации объекта от внешнего кода.
  Доступ к данным осуществляется через публичные методы (getters/setters).
  Это уменьшает связанность кода и защищает состояние объекта.


  **2. Наследование**
  Позволяет одному классу наследовать свойства и методы другого.
  Это даёт возможность переиспользовать код и расширять поведение.
  (Лучшие и самые показательные примеры - могут быть на бэкэнде)

    Человек-учитель
    Человек-ученик

    У того и того есть свойства человека, но есть также отличия.



  В JavaScript наследование реализуется через прототипы ([[Prototype]]), а в TypeScript — синтаксически через class ... extends.

  **3. Полиморфизм**
  Возможность вызывать один и тот же метод у разных объектов, но с разной реализацией.
  Позволяет писать код, не зная конкретный тип объекта.

  ```typescript
  function makeSound(animal: Animal) {
    animal.speak();
  }

  makeSound(new Dog()); // "Woof!"
  makeSound(new Animal()); // "Some sound"
  ```


  В TypeScript это реализуется через переопределение методов и интерфейсы.

  **4. Абстракция**
  Сокрытие сложных деталей и выделение только значимых характеристик.
  Часто реализуется через абстрактные классы и интерфейсы.

  ```typescript
  interface Shape {
    area(): number;
  }

  class Circle implements Shape {
    constructor(private radius: number) {}
    area() {
      return Math.PI * this.radius ** 2;
    }
  }
  ```


  Так программист работает с концепцией “фигура”, не вдаваясь в детали конкретных реализаций.

  **🔸 Особенности именно JS/TS**

  В JavaScript классы — это синтаксический сахар над прототипами.
  Под капотом всё работает через прототипное наследование.

  В TypeScript добавлены модификаторы доступа (private, protected, public), интерфейсы, абстрактные классы — то, чего нет в чистом JS.
  Это делает ООП ближе к классическим языкам вроде Java или C#.

  TS позволяет типизировать ООП-конструкции, что повышает надёжность и читаемость кода.

## 4) Что происходит, когда я ввожу URL в браузере

  1. Ввод адреса
    Браузер понимает, что ты ввёл — сайт, поиск или локальный файл. Если сайт — добавляет `https://`, если нужно.

  2. DNS (поиск IP)
    Чтобы понять, где находится сайт, браузер спрашивает DNS-сервер:
    «Какой IP у example.com?»
    Ответ — IP-адрес сервера, где находится сайт.

  3. Установка соединения
    Браузер соединяется с сервером:

    * открывает TCP-соединение,
    * шифрует его через TLS (чтобы был HTTPS),
    * договаривается о протоколе (HTTP/1.1, HTTP/2, HTTP/3).

  4. Отправка запроса
    Браузер шлёт запрос: `GET /`, вместе с заголовками (cookies, язык и т.п.).

  5. Ответ сервера
    Сервер присылает HTML-страницу. Может отдать и дополнительные файлы — CSS, JS, изображения. Иногда перенаправляет на другой адрес.

  6. Парсинг и построение страницы
    Браузер:

    * читает HTML → создаёт DOM-дерево;
    * загружает CSS → строит CSSOM;
    * объединяет их в Render Tree;
    * считает размеры и позиции (Layout);
    * рисует (Paint) и выводит на экран (Compositing).

  7. Выполнение JavaScript
    Скрипты меняют DOM, обрабатывают события, делают запросы (`fetch`, `ajax`), перерисовывают элементы и т.д.

  8. Отрисовка пользователю
    После первых стилей и контента пользователь видит страницу. Потом догружается остальное (изображения, шрифты, интерактивность).

  ---

  ⚡ Что помогает сделать это быстрее

  * DNS cache — не ищем IP каждый раз.
  * TLS 1.3, HTTP/2, HTTP/3 (QUIC) — меньше задержек и быстрее соединение.
  * CDN — сервер ближе к пользователю.
  * Кеширование (`Cache-Control`, `ETag`) — не качаем повторно.
  * Preload, preconnect, prefetch — заранее подготавливаем нужные файлы.
  * Service Worker — офлайн-кеш, мгновенные повторные загрузки.
  * Минификация и lazy-loading — меньше данных и меньше блокировок при загрузке.
  * Server Side Rendering — самое современное - это когда страницы полностью рендерятся на сервере и сервер отдаёт уже полную html страницу со всей отрисовкой. Работает быстро, т.к. мощности сервере больше, чем мощность клиентского устройства, но создаёт нагрузку на сервер. Очень хорошо работает для лендингов. Пока также имеет свои ограничения, т.к. многие библиотеки по умолчанию предполагают, что они рендерятся именно в браузере, а не на сервере.

